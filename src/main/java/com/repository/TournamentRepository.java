package com.repository;

import org.springframework.data.jpa.repository.JpaRepository;

/*public interface TournamentRepository extends JpaRepository<Tournament, Integer> {

    void setWinTeam(int teamId, int idTourmament);

    void setTeamRaiting(int teamId, double points);

    void updateMatchSecondTeam(int idTourmament, int matchNumber, int secondTeamId);

    boolean checkMatch(int idTourmament, int matchNumber);

    void updateMatchFirstTeam(int idTourmament, int matchNumber, int firstTeam);

    void createMatch(int idTourmament, int round, int matchNumber, int firstTeamId, int secondTeamId);
}*/
